{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"process.stdout.write(require('fs').readFileSync(require('path').join('${CLAUDE_PLUGIN_ROOT}','ascii-banner.txt'),'utf8'))\""
          }
        ]
      }
    ]
  }
}
